providers = ["node"]

[phases.install]
cmds = ["cd backend && npm ci --omit=dev"]

[phases.build]
cmds = ["cd backend && npx prisma generate"]

[start]
cmd = "cd backend && node prisma/migrate.js && node prisma/seed.js && node src/index.js"
