{
  "name": "sdmeet-game",
  "version": "1.0.0",
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate",
    "start": "cd backend && node prisma/migrate.js && node prisma/seed.js && node src/index.js"
  }
}
